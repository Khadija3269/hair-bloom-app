services:

  backend:
    build: ./server
    ports:
      - "5000:5000"
    restart: on-failure

  frontend:
    build: ./client
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: on-failure
